<h1>Create New Song</h1>
<% if logged_in? %>
  <%= form_for @song, url: {action: "create"} do |f| %>
    Title <%= f.text_field :title %>
    Artist <%= f.text_field :artist %>
    URL <%= f.text_field :url %>
    <%= f.submit %>
  <% end %>
<% else %>
  <h3>Please log in to add a new song</h3>
<% end %>
